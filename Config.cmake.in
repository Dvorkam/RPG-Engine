@PACKAGE_INIT@
set(PACKAGE_NAME RPGEngine)
find_package(doctest CONFIG REQUIRED)

find_path(${PACKAGE_NAME}_TARGETS_PATH 
	NAMES ${PACKAGE_NAME}Targets.cmake
	PATHS ${CMAKE_CURRENT_LIST_DIR}/
	PATH_SUFFIXES cmake ${PACKAGE_NAME} ${PACKAGE_NAME}/cmake
)

include(${${PACKAGE_NAME}_TARGETS_PATH}/${PACKAGE_NAME}Targets.cmake)
